import frida = require('frida');
export default class EasyFrida {
    target: number | string;
    location: 'usb' | 'local' | 'remote';
    targetos: 'win' | 'linux' | 'android' | 'ios';
    remoteAddr?: string;
    compileOptions: {
        bytecode: boolean;
        babelify: boolean;
        esmify: boolean;
        loose: boolean;
        sourcemap: boolean;
        compress: boolean;
        useAbsolutePaths: boolean;
    };
    baseDir: string;
    agentProjectDir: string;
    outFile: string;
    logFile?: string;
    scriptFile?: string;
    device?: frida.Device;
    private curProc;
    private procList;
    private interacting;
    private fridaRepl;
    private scopeDepth;
    private prompt;
    private remoteEvalCallback;
    private enableChildGating;
    constructor(target: number | string, location: 'usb' | 'local' | 'remote', targetos: 'win' | 'linux' | 'android' | 'ios', remoteAddr?: string);
    run(target?: string | number, enableChildGating?: boolean): Promise<boolean>;
    attach(target?: string | number, enableChildGating?: boolean): Promise<boolean>;
    attachOrRun(target?: string | number, enableChildGating?: boolean): Promise<boolean>;
    inject(file?: string, target?: string | number, enableChildGating?: boolean): Promise<void>;
    resume(pid?: number): void;
    getDevice(): Promise<frida.Device>;
    private attachToSession;
    reload(): Promise<void>;
    compile(file?: string): Promise<unknown>;
    load(file?: string): Promise<void>;
    watch(file?: string, target?: string | number, enableChildGating?: boolean): Promise<void>;
    interact(finallyKill?: boolean): Promise<void>;
    private localEval;
    private remoteEval;
    private onChild;
    private onCrashed;
    private log;
    detach(): Promise<void>;
    kill(): Promise<void>;
    private onMessage;
    private updatePrompt;
}
